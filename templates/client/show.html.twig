{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}

{% block body %}
    <h1>Client</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>Nomc</th>
                <td>{{ client.nomc }}</td>
            </tr>
            <tr>
                <th>Creditc</th>
                <td>{{ client.creditc }}</td>
            </tr>
            <tr>
                <th>Adrc</th>
                <td>{{ client.adrc }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-dark" href="{{ path('app_client_index') }}">back to list</a>

    <a class="btn btn-info" href="{{ path('app_client_edit', {'id': client.id}) }}">edit</a>

    {{ include('client/_delete_form.html.twig') }}
{% endblock %}
